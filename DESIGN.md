# 디자인 시스템

## Authority / mode
시각 참고는 OpenAI Platform 대시보드(2026-09-25 사용자 제공 화면)다. 앱은 Operate(검색·실행·분석), 데이터 안내는 Read 모드다. 이전 Coinbase 참고(docs/references/DESIGN-coinbase.md)는 대체되었다.

## Adopted
회색 바탕 #f3f3f3 위 흰 메인 패널(반경 16px, 1px #e8e8e8), 왼쪽 232px 사이드바와 상단 이동 경로. 글자 #0d0d0d, 보조 #5d5d5d, 흐림 #8f8f8f. 주 버튼은 검정 알약, 보조는 연회색(#f0f0f0) 알약, 선택 칩은 검정 채움. 중앙 검색 입력(52px 알약, 옅은 그림자)과 추천 칩, 그 아래 카드 목록.

## Adapted
색 강조는 시장 색만 쓴다: 상승 #e5383b, 하락 #2563eb, 항상 부호·▲▼와 함께. 파란 강조색은 하락과 혼동되므로 쓰지 않는다. 글꼴은 Pretendard(자체 호스팅), 숫자는 tabular-nums. 제목 28px/600, 섹션 17px/600, 본문 14px. 이동평균선 색은 5일 #f59f00, 20일 #12b886, 60일 #7048e8, 120일 #868e96.

## Composition
홈: "어떤 종목을 분석할까요?" 검색 → 저장된 종목 칩 → 최근 실험 카드. 종목 작업 공간: 머리글, 탭(과거 백테스트 / 미래 예측). 백테스트는 왼쪽 300px 설정(기간 칩·전략 카드·자금·접힌 고급 설정)과 오른쪽 결과(요약 문장, 지표 4칸, 가격 차트, 자산 차트, 왕복 매매 표). 예측은 기간 선택, 예상 가격, 검증 지표 3칸, 판정 문장, 예측 점선 차트. 가격 차트는 캔들·OHLC 막대·선·영역 전환과 이동평균 토글을 공유한다.

## Responsive / accessibility
1100px 이하 설정·결과 단일 열. 760px 이하 사이드바가 가로 스크롤 메뉴가 된다. 주 버튼 44px, 칩 36px. focus-visible 2px 검정 외곽선. 검색은 combobox/listbox와 방향키·Enter·Esc. 탭은 tab 역할, 토글은 aria-pressed. 오류 alert, 진행 상태 status. 차트 범례 대신 이동평균 토글의 색 표시와 차트 아래 설명 한 줄.

## States
수집 중(약 5초 안내), 계산 중, 완료(결과 자동 표시), 실패, 취소, 오래된 결과(30일), 가상 데이터를 텍스트로 구별한다. 공개 모드에는 검색·실행·예측이 없고 공개 결과 카드와 결과 보기만 있다. 예측에는 검증 수치와 "투자 조언이 아님" 안내를 항상 함께 둔다.
